% RefineFrequency: applies a frequency refinement algorithm (continuous
% power spectrum)
%
%   oldfreq: frequency value to be refines
%   x        input signal
%   options  options struct
function freq = RefineFrequency(oldfreq, x, options)
end